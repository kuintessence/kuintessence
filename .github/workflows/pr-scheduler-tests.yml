name: PR scheduler tests

on:
  pull_request:
    branches: [main]
    types: [opened, synchronize, reopened, ready_for_review]
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: pr-scheduler-tests-${{ github.event_name }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  spack-managed:
    name: Spack GNU Hello managed installation
    if: >-
      github.event_name == 'workflow_dispatch' ||
      (github.event.pull_request.head.repo.full_name == github.repository &&
       !github.event.pull_request.draft)
    runs-on: ubuntu-24.04
    timeout-minutes: 70
    steps:
      - uses: actions/checkout@v4
        with:
          persist-credentials: false
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3
      - name: Verify isolated managed install, restart and uninstall
        run: timeout --signal=TERM --kill-after=60s 65m bash deploy/pr-test/run.sh slurm --spack-managed

  spack-case:
    name: Spack GNU Hello single-step case
    if: >-
      github.event_name == 'workflow_dispatch' ||
      (github.event.pull_request.head.repo.full_name == github.repository &&
       !github.event.pull_request.draft)
    runs-on: ubuntu-24.04
    timeout-minutes: 60
    steps:
      - uses: actions/checkout@v4
        with:
          persist-credentials: false
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3
      - name: Deliver materials, compile offline and run one Slurm step
        run: timeout --signal=TERM --kill-after=60s 55m bash deploy/pr-test/run.sh slurm --spack-case

  scheduler:
    if: >-
      github.event_name == 'workflow_dispatch' ||
      (github.event.pull_request.head.repo.full_name == github.repository &&
       !github.event.pull_request.draft)
    runs-on: ubuntu-24.04
    timeout-minutes: 45
    strategy:
      fail-fast: false
      matrix:
        scheduler: [slurm, pbs]
    steps:
      - uses: actions/checkout@v4
        with:
          persist-credentials: false
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3
      - name: Build and test disposable scheduler stack
        env:
          SCHEDULER: ${{ matrix.scheduler }}
        run: timeout --signal=TERM --kill-after=60s 40m bash deploy/pr-test/run.sh "$SCHEDULER"
