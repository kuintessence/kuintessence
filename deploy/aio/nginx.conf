map $http_authorization $registry_authorization {
    default $http_authorization;
    "" "Bearer $cookie_kq_access_token";
}

server {
    listen 80 default_server;
    server_name _;
    root /var/www;
    index index.html;

    # Health probe convenience: /healthz returns 200 once nginx is up
    location = /healthz {
        access_log off;
        return 200 "ok\n";
    }

    location /platform/api/ {
        rewrite ^/platform(/.*)$ $1 break;
        proxy_pass http://127.0.0.1:3000;
        proxy_cookie_path /api/auth/oidc /platform/api/auth/oidc;
        proxy_cookie_path /api/auth /platform/api/auth;
        proxy_http_version 1.1;
        proxy_set_header Host              $host;
        proxy_set_header X-Real-IP         $remote_addr;
        proxy_set_header X-Forwarded-For   $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header Upgrade           $http_upgrade;
        proxy_set_header Connection        "upgrade";
    }

    location /platform/ws/ {
        rewrite ^/platform(/.*)$ $1 break;
        proxy_pass http://127.0.0.1:3000;
        proxy_http_version 1.1;
        proxy_set_header Host              $host;
        proxy_set_header X-Real-IP         $remote_addr;
        proxy_set_header X-Forwarded-For   $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header Upgrade           $http_upgrade;
        proxy_set_header Connection        "upgrade";
    }

    # Legacy direct API prefix for existing CLI and development clients.
    location ^~ /api/agent/spack/operations/ {
        proxy_pass http://127.0.0.1:3000;
        proxy_http_version 1.1;
        proxy_buffering off;
        proxy_read_timeout 300s;
        proxy_set_header Authorization $http_authorization;
        proxy_set_header Host $host;
    }

    location /api/ {
        proxy_pass http://127.0.0.1:3000;
        proxy_http_version 1.1;
        proxy_set_header Host              $host;
        proxy_set_header X-Real-IP         $remote_addr;
        proxy_set_header X-Forwarded-For   $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header Upgrade           $http_upgrade;
        proxy_set_header Connection        "upgrade";
    }

    # Raw bundles use Registry's 128 MiB limit; queued Git validation can be slow.
    location = /software/api/spack/recipe-repositories/import {
        client_max_body_size 128m;
        proxy_request_buffering off;
        proxy_read_timeout 900s;
        proxy_send_timeout 900s;
        rewrite ^/software/(.*)$ /$1 break;
        proxy_pass http://127.0.0.1:3100;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header Authorization $registry_authorization;
        proxy_set_header X-Real-IP $remote_addr;
    }

    # Proxy /software/api → Registry (strip /software prefix only, keep /api).
    # Only matches /software/api/*, leaving /software (SPA route) to fall through
    # to the SPA fallback below. Without this exact prefix, nginx auto-301s
    # /software → /software/ and the SPA's Software page is never reachable.
    location ^~ /software/api/spack/material-repositories/ {
        client_max_body_size 16g;
        proxy_request_buffering off;
        proxy_buffering off;
        proxy_read_timeout 1800s;
        proxy_send_timeout 1800s;
        rewrite ^/software/(.*)$ /$1 break;
        proxy_pass http://127.0.0.1:3100;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header Authorization $registry_authorization;
        proxy_set_header X-Real-IP $remote_addr;
    }

    location /software/api/ {
        rewrite ^/software/(.*)$ /$1 break;
        proxy_pass http://127.0.0.1:3100;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header Authorization $registry_authorization;
        proxy_set_header X-Real-IP $remote_addr;
    }

    location /v2/ {
        proxy_pass http://127.0.0.1:3100;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header Authorization $registry_authorization;
        proxy_set_header X-Real-IP $remote_addr;
    }

    location /buildcache/ {
        proxy_pass http://127.0.0.1:3100;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header Authorization $registry_authorization;
        proxy_set_header X-Real-IP $remote_addr;
    }

    # Stable branding assets are copied from packages/web/public by Vite.
    # Keep this path explicit so unknown branding paths fail as 404 instead of
    # being served as index.html by the SPA fallback.
    location /branding/ {
        try_files $uri =404;
        add_header Cache-Control "public, max-age=3600";
    }

    # Long-cache hashed assets emitted by Vite
    location /assets/ {
        expires 1y;
        add_header Cache-Control "public, immutable";
    }

    # SPA fallback — every unknown path resolves to index.html
    location / {
        try_files $uri $uri/ /index.html;
    }
}
