/** Single source of truth for the `kq` CLI version — used by Commander's
 *  `--version` and surfaced in the TUI help overlay. Keep in sync with
 *  package.json's `version` on release. */
export const KQ_VERSION = "0.1.0-rc.1";
