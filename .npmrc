# Keep dependency resolution independent of a developer or runner's global npm config.
# This repository contains only public npm packages; do not add credentials here.
registry=https://registry.npmjs.org/
