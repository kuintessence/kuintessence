{
  "name": "@kuintessence/proto",
  "license": "AGPL-3.0-only",
  "version": "0.1.0-rc.1",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": {
      "bun": "./src/index.ts",
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "scripts": {
    "generate": "buf generate",
    "prepare": "buf generate",
    "typecheck": "buf generate && tsc --noEmit",
    "test": "buf generate && bun test",
    "lint:proto": "buf lint",
    "clean": "rm -rf src/generated dist"
  },
  "dependencies": {
    "@bufbuild/protobuf": "^2.12.0",
    "@connectrpc/connect": "^2.1.1"
  },
  "devDependencies": {
    "@bufbuild/buf": "^1.68.4",
    "@bufbuild/protoc-gen-es": "^2.12.0"
  }
}
